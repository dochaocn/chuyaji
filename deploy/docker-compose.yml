# 可选：仅作本地/单机编排参考；生产请自行加固镜像与密钥管理
services:
  api:
    image: alpine:3.20
    working_dir: /app
    volumes:
      - ../:/app:ro
    command: ["/bin/sh", "-c", "echo '请改为使用自行构建的 chuyaji-api 镜像或挂载编译好的二进制'; sleep 3600"]
    ports:
      - "8282:8282"
